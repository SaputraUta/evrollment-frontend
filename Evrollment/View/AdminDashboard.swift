import SwiftUI

struct AdminDashboard: View {
    var body: some View {
        VStack {
            Text("Welcome Admin!")
                .font(.largeTitle)
                .bold()
            Spacer()
        }
        .padding()
    }
}
