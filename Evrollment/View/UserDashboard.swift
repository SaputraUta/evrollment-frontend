import SwiftUI

struct UserDashboard: View {
    var body: some View {
        VStack {
            Text("Welcome User!")
                .font(.largeTitle)
                .bold()
            Spacer()
        }
        .padding()
    }
}
